## Что меняем в `src/routes/index.tsx` (только секция `FeaturedProjects`)

1. **Фон секции** — заменить тёмный `bg-hero-gradient` на светлый в стиле референса:
   - Секция получит светлый фон (`bg-[oklch(0.98_0.005_300)]`), убираем фиолетовый радиальный blur.
   - Текст заголовка и описаний внутри секции переопределяем на тёмный (`text-[oklch(0.14_0.03_300)]`), подписи — на приглушённо-серый.
   - Бейдж «Портфолио» и теги технологий — светлая карточка с тёмным текстом и мягкой границей.
   - Акцент «проекты» в заголовке оставляем неоново-лаймовым, но без светящегося shadow (на светлом фоне он выглядит грязно) — просто цветной текст с лёгким контурным свечением.
   - Карточки проектов: фон `bg-white` вместо `bg-card/40`, граница светлее, текст внутри карточек тёмный, теги — светло-серые пилюли. Превью-изображения и hover-эффекты остаются без изменений.

2. **Ссылка «Посмотреть все проекты»** в правом верхнем углу секции:
   - Разместить в существующем header-ряду секции (там уже flex-контейнер с заголовком слева).
   - Обычный текст-ссылка `<a href="#projects">` — не кнопка: тёмный цвет, подчёркивание при hover, стрелка `ArrowUpRight` рядом.
   - На мобильных остаётся в том же ряду, компактно справа сверху над карточками (заголовок слева, ссылка справа).

Остальные секции (Hero, шапка, стили, ассеты) не трогаем.
